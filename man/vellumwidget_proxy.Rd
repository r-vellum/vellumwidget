% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proxy.R
\name{vellumwidget_proxy}
\alias{vellumwidget_proxy}
\title{Drive an already-rendered widget from the Shiny server}
\usage{
vellumwidget_proxy(outputId, session = NULL)
}
\arguments{
\item{outputId}{The id of the \code{\link[=vellumwidgetOutput]{vellumwidgetOutput()}} to control (the un-namespaced
id, as passed to \code{vellumwidgetOutput()}; module namespacing is handled for you).}

\item{session}{The Shiny session; defaults to the current reactive domain, so
you rarely pass it explicitly.}
}
\value{
An object of class \code{"vellumwidget_proxy"} (invisibly from the verbs), to be
piped into \code{\link[=vw_select]{vw_select()}} and the other proxy verbs.
}
\description{
\code{vellumwidget_proxy()} is the server-to-client counterpart of the input
read-back documented in \link{vellumwidget-shiny}. It returns a lightweight handle to a
widget that is \strong{already on the page}, so the server can change what the
widget shows --- set the selection, cross-filter it, zoom it --- \strong{without
re-rendering} it (no \code{renderVellumwidget()} round-trip, no full redraw, no lost
pan/zoom). This mirrors \code{leaflet::leafletProxy()} / \code{DT::dataTableProxy()} /
\code{plotly::plotlyProxy()}.
}
\details{
Call it inside an \code{observe()} / \code{observeEvent()} with the same \code{outputId} you
gave \code{\link[=vellumwidgetOutput]{vellumwidgetOutput()}}, then pipe the handle through one or more of the verbs
below. Each verb sends a single custom message to the browser and returns the
proxy invisibly, so calls chain with the pipe.

\describe{
\item{\code{\link[=vw_select]{vw_select()}}}{Replace the widget's selection with \code{keys} (the element
\code{data_id}s). Selecting projects across a mark's \code{hover_group} and
propagates to any linked / crosstalk widgets, exactly as a user click
would, and updates \verb{input$<id>_selected}.}
\item{\code{\link[=vw_clear_selection]{vw_clear_selection()}}}{Clear the selection.}
\item{\code{\link[=vw_filter]{vw_filter()}}}{Cross-filter: show only the elements whose key is in
\code{keys} and dim/hide the rest (display tier --- the data is untouched).}
\item{\code{\link[=vw_clear_filter]{vw_clear_filter()}}}{Remove the filter (show everything).}
\item{\code{\link[=vw_zoom]{vw_zoom()}}}{Zoom/pan the view to frame the elements in \code{keys}.}
\item{\code{\link[=vw_reset_zoom]{vw_reset_zoom()}}}{Restore the original (full) view.}
}

All keys are the element \strong{data keys} --- the \code{data_id} a \code{vellumplot} mark
declares --- the same identifiers you receive back through \verb{input$<id>_selected}
and friends.
}
\examples{
\dontrun{
library(shiny)
server <- function(input, output, session) {
  output$plot <- renderVellumwidget(my_widget)
  # A server-side control drives the plot without redrawing it:
  observeEvent(input$highlight, {
    vellumwidget_proxy("plot") |> vw_select(input$highlight)
  })
  observeEvent(input$reset, {
    vellumwidget_proxy("plot") |> vw_clear_selection() |> vw_reset_zoom()
  })
}
}
}
\seealso{
\code{\link[=vw_select]{vw_select()}}, \code{\link[=vw_filter]{vw_filter()}}, \code{\link[=vw_zoom]{vw_zoom()}}; \code{\link[=vellumwidgetOutput]{vellumwidgetOutput()}} and
\link{vellumwidget-shiny} for reading interactions back.
}
